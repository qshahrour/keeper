---

- name: Start PM2
  shell:
    "cd /var/www/ingotbrokers-portal && pm2 start all"
  args:
    chdir: "{{ dest_dir[3] }}"
  register: PM2

- name: Debug PM2 Start
  debug:
    msg:
      - "started {{ PM2.stdout_lines }}"

---

- name: check Install Node and NPM
  shell:
    "npm -v"
  args:
    chdir: "$HOME"
    #chdir: "{{ dest_dir[1] }}"
  register: RESULT

#- name: Validate if the installation is intact
#assert:
#that: "RESULT is changed"
  
- name: Show STDOUT_LINES
  debug:
    msg: 
      - " changed {{ RESULT.stdout_lines }}"

#- name: Check Install Node and NPM
#shell:
#  "npm -v && nodejs -v"
#chdir: "{{ dest_dir[3] }}"
#register: NODE_VERSION
#
#- name: Install PM2
#npm:
#name: pm2
#global: yes

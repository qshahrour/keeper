---

- name: Start DB batch Upload
  become: true
  async: 10
  poll: 0
  shell:
    "./startbashupload.sh"
  args:
    chdir: "/apps/dbscripts/"
    creates: "startbatchupload.lock"
  register: fileout

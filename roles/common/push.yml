---

- name: Copy files
  become: true
  synchronize:
    src: "{{ sync_dir[0] }}"
    dest: "{{ sync_dir[5] }}"
    dirs: yes
    mode: push
    recursive: ye
    archive: no
    checksum: yes
  register: sync_sucess

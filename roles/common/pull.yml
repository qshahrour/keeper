---

- name: Pull Directory Files
  become: true
  synchronize:
    src: "{{ sync_dir[0] }}"
    dest: "{{ sync_dir[0] }}"
    dirs: yes
    mode: pull
    recursive: yes
    archive: no
    checksum: yes

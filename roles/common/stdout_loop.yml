---

- name: Show lines with TRUE only
  debug:
    msg: "{{ item }}"
  when: "'installed' in item"
  loop: "{{ RESULT.stdout_lines }}"

- name: Show lines with TRUE and line before
  debug:
    msg: "{{ RESULT.STDOUT_LINES[ansible_loop.index0 - 1] }}\n{{ item }}"
  when: "'installed' in item"
  loop: "{{ RESULT.stdout_lines }}"
  loop_control:
    extended: true
    label: "{{ ansible_loop.index0 }}"
